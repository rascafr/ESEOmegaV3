<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/md_blue_800</item>
        <item name="colorPrimaryDark">@color/md_blue_900</item>
        <item name="colorAccent">@color/color_primary</item>

        <!-- Preferences Theme -->
        <!--<item name="preferenceTheme">@style/PreferenceThemeOverlay.</item>-->
        <item name="preferenceTheme">@style/PreferenceThemeOverlay.v14.Material</item>
    </style>

    <!-- single activity theme. -->
    <style name="AppThemeSingle" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/md_blue_800</item>
        <item name="colorPrimaryDark">@color/md_blue_900</item>
        <item name="colorAccent">@color/color_primary</item>
    </style>

    <!-- Splash activity theme. -->
    <style name="SplashTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/md_blue_800</item>
        <item name="colorPrimaryDark">@color/color_splash</item>
        <item name="colorAccent">@color/color_primary</item>
    </style>

    <!-- Image view activity theme. -->
    <style name="ImageViewTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/md_grey_800</item>
        <item name="colorPrimaryDark">@color/md_grey_900</item>
        <item name="colorAccent">@color/color_primary</item>
    </style>

    <!-- Lydia Activity - Dialog like theme -->
    <style name="AppThemeLydia" parent="Theme.AppCompat.Light.Dialog">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@color/backgournd_lydia_dial</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:backgroundDimEnabled">true</item>
    </style>

</resources>
